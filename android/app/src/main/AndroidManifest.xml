<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      allowBackup is an explicit choice, not an inherited default
      (android_release_plan.md 2.3). StorageService keeps the high score and
      every Settings value in shared_preferences, and Android Auto Backup
      carrying those to a new device is a feature: a player who changes phones
      keeps their best score. There is no account, no secret and no server-side
      state here, so nothing is unsafe to restore. Set this to false only if you
      decide a fresh install should mean a fresh high score.
    -->
    <application
        android:label="Tetrofall"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- AdMob app ID — required by google_mobile_ads regardless of which ad
             formats are in use. See lib/services/ad_unit_ids.dart for the
             per-format unit IDs (test IDs are swapped in for debug builds). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5422471961828877~5855001448"/>
        <!-- Firebase Analytics consent-mode defaults. All three ad signals
             start DENIED so a cold start in the EEA measures nothing
             ad-related before the UMP form has been answered; AdsService
             grants them the moment consent resolves (see _publishConsent and
             FirebaseAnalyticsService.setConsent). Outside the regions UMP
             covers the form is never required and the grant lands within a
             second of launch, so this costs no attribution there.

             analytics_storage is deliberately absent, which leaves it at the
             SDK default of granted: it covers first-party measurement, and
             denying it would mean no analytics at all rather than
             un-personalised analytics. -->
        <meta-data
            android:name="google_analytics_default_allow_ad_storage"
            android:value="false"/>
        <meta-data
            android:name="google_analytics_default_allow_ad_user_data"
            android:value="false"/>
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false"/>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package visibility for url_launcher, which Settings → Privacy
             Policy uses to hand the hosted policy to a browser. Without this,
             Android 11+ hides every browser from the app and the launch is
             reported as failed rather than opening anything. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
